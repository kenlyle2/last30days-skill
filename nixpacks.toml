[phases.setup]
nixPkgs = ["python312", "nodejs_20", "yt-dlp"]

[phases.install]
cmds = ["python3 -m ensurepip --upgrade", "python3 -m pip install -e ."]

[start]
cmd = "python3 -m ensurepip --upgrade && python3 -m pip install -e . && uvicorn api:app --host 0.0.0.0 --port ${PORT:-8080}"
