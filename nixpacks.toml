[phases.setup]
nixPkgs = ["python312", "nodejs_20", "yt-dlp"]

[phases.install]
cmds = ["uv pip install --system -e ."]

[start]
cmd = "uv pip install --system -e . && uvicorn api:app --host 0.0.0.0 --port ${PORT:-8080}"
