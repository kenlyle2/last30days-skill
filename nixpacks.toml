[phases.setup]
nixPkgs = ["python312", "nodejs_20", "yt-dlp"]

[phases.install]
cmds = ["python3 -m venv /opt/venv", "/opt/venv/bin/pip install 'fastapi>=0.115' 'uvicorn[standard]>=0.30'"]

[start]
cmd = "/opt/venv/bin/uvicorn api:app --host 0.0.0.0 --port ${PORT:-8080}"
