{
  "$schema": "https://schema.railway.app/railway-schema-v2.json",
  "deploy": {
    "startCommand": "/opt/venv/bin/uvicorn api:app --host 0.0.0.0 --port ${PORT:-8080}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  },
  "build": {
    "builder": "NIXPACKS"
  }
}
